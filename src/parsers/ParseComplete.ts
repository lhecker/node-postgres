import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = '1';
export default function Parser$ParseComplete(conn: Connection, reader: MessageReader) {
}

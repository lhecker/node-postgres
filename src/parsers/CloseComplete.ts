import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = '3';
export default function Parser$CloseComplete(conn: Connection, reader: MessageReader) {
}

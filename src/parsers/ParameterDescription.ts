import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = 't';
export default function Parser$ParameterDescription(conn: Connection, reader: MessageReader) {
    throw new Error('unimplemented');
}

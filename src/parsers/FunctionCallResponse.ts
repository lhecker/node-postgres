import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = 'V';
export default function Parser$FunctionCallResponse(conn: Connection, reader: MessageReader) {
    throw new Error('unimplemented');
}

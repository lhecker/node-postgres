import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = '2';
export default function Parser$BindComplete(conn: Connection, reader: MessageReader) {
}

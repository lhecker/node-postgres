import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = 'W';
export default function Parser$CopyBothResponse(conn: Connection, reader: MessageReader) {
    throw new Error('unimplemented');
}

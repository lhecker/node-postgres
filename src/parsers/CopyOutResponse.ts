import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = 'H';
export default function Parser$CopyOutResponse(conn: Connection, reader: MessageReader) {
    throw new Error('unimplemented');
}

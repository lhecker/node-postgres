import Connection from '../Connection';
import MessageReader from '../MessageReader';

export const type = 'A';
export default function Parser$NotificationResponse(conn: Connection, reader: MessageReader) {
    throw new Error('unimplemented');
}
